# 앱 아이콘 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tauri 스캐폴딩 기본 아이콘을 앱을 대표하는 이중 링 게이지 아이콘으로 교체하고, 재생성을 스크립트로 고정한다.

**Architecture:** 손으로 그린 SVG 두 장(컬러 마스터, 트레이용 단색 마스터)이 유일한 원본이다. Playwright Chromium이 이를 PNG로 렌더하고, `tauri icon`이 1024px PNG에서 번들 아이콘 세트 전체를 파생한다. 생성물은 빌드에 필요하므로 커밋한다. macOS 트레이만 단색 템플릿 아이콘으로 분기한다.

**Tech Stack:** SVG, Playwright(기존 devDependency), `@tauri-apps/cli`의 `tauri icon`, Vitest, Rust/Tauri v2

**설계 문서:** `docs/superpowers/specs/2026-07-24-app-icon-design.md`

## Global Constraints

- 좌표계는 128×128 기준. 판은 라운드 사각 `rx=28`, 세로 그라디언트 `#2E2E33` → `#141416`.
- 바깥 링: 중심 (64, 64), `r=47`, `stroke-width=14`, `stroke="#D97757"`, `stroke-dasharray="201 296"`.
- 안쪽 링: 중심 (64, 64), `r=22`, `stroke-width=14`, `stroke="#5162ED"`, `stroke-dasharray="58 139"`.
- 트랙 색 `#3A3A3C`. 링은 `fill="none"`, `stroke-linecap="round"`, 그룹에 `transform="rotate(-90 64 64)"`(12시에서 시계 방향).
- 위 수치는 승인된 시안 그대로다. **임의로 바꾸지 않는다.**
- 새 npm/cargo 의존성을 추가하지 않는다. 단 `tauri` 크레이트의 `image-png` 기능 플래그는 켠다.
- 주석과 커밋 메시지 본문은 한국어, 커밋 제목은 Conventional Commits 영문 접두사를 쓴다(기존 이력과 동일).
- 모든 렌더는 `omitBackground: true`로 뽑는다. 판의 둥근 모서리가 투명해야 OS가 아이콘을 제대로 마스킹한다.

## File Structure

| 파일 | 책임 |
| --- | --- |
| `src-tauri/icons/source/app-icon.svg` | 컬러 마스터. 모든 번들 아이콘과 favicon의 원본 |
| `src-tauri/icons/source/tray-template.svg` | 트레이용 단색 마스터. 알파 농담만으로 트랙과 진행분을 구분 |
| `scripts/icons/png.ts` | PNG 헤더에서 크기를 읽는 순수 함수. 렌더 스크립트와 테스트가 공유 |
| `scripts/icons/render.ts` | SVG → PNG 렌더와 파생 산출물 복사. 출력 크기를 스스로 검증 |
| `scripts/icons/icons.test.ts` | 커밋된 아이콘 산출물이 빠짐없이, 올바른 크기로 있는지 검사 |
| `src-tauri/src/lib.rs` | 트레이 아이콘을 플랫폼별로 분기 |
| `index.html` | favicon과 문서 제목의 스캐폴딩 잔재 정리 |
| `README.md` | 아이콘 노출과 재생성 방법 안내 |

---

### Task 1: 마스터 SVG와 렌더 스크립트

**Files:**
- Create: `src-tauri/icons/source/app-icon.svg`
- Create: `src-tauri/icons/source/tray-template.svg`
- Create: `scripts/icons/png.ts`
- Create: `scripts/icons/render.ts`
- Create: `scripts/icons/icons.test.ts`
- Modify: `package.json` (scripts에 `icons` 추가)
- Modify: `.gitignore`

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces:
  - `pngSize(path: string): Promise<{ width: number; height: number }>` — `scripts/icons/png.ts`에서 export
  - 산출 파일: `scripts/icons/.tmp/app-icon-1024.png`(1024×1024), `src-tauri/icons/tray/tray-template.png`(44×44), `docs/images/app-icon.png`(128×128), `public/app-icon.svg`(마스터 사본)

- [ ] **Step 1: 의존성을 설치한다**

이 워크트리에는 `node_modules`가 없다. Playwright와 `tauri` CLI 둘 다 필요하다.

```bash
npm ci
npx playwright install chromium
```

Expected: 설치 완료. `ls node_modules/.bin/tauri` 가 경로를 출력한다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

Create `scripts/icons/icons.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { pngSize } from "./png.ts";

describe("렌더된 아이콘 산출물", () => {
  it("트레이 템플릿은 레티나 기준 44×44다", async () => {
    expect(await pngSize("src-tauri/icons/tray/tray-template.png")).toEqual({
      width: 44,
      height: 44,
    });
  });

  it("README용 아이콘은 128×128이다", async () => {
    expect(await pngSize("docs/images/app-icon.png")).toEqual({ width: 128, height: 128 });
  });
});
```

`scripts/icons/.tmp/app-icon-1024.png`는 **여기서 검사하지 않는다.** 그 파일은
`.gitignore` 대상이라 저장소를 새로 받은 사람이나 CI에는 존재하지 않고, 크기 검증은
`render.ts`가 뽑는 즉시 스스로 한다(Step 7). 커밋되는 산출물만 테스트가 지킨다.

- [ ] **Step 3: 테스트가 실패하는지 확인한다**

Run: `npx vitest run scripts/icons/icons.test.ts`
Expected: FAIL — `Failed to resolve import "./png.ts"`

- [ ] **Step 4: PNG 크기 판독기를 만든다**

Create `scripts/icons/png.ts`:

```ts
import { readFile } from "node:fs/promises";

/**
 * PNG의 IHDR 청크에서 가로·세로를 읽는다. 렌더 결과가 의도한 크기인지 확인하는 데는
 * 이것으로 충분하므로 이미지 라이브러리를 들이지 않는다.
 */
export async function pngSize(path: string): Promise<{ width: number; height: number }> {
  const buf = await readFile(path);
  if (buf.length < 24 || buf.readUInt32BE(0) !== 0x89504e47) {
    throw new Error(`${path}: PNG 파일이 아닙니다.`);
  }
  return { width: buf.readUInt32BE(16), height: buf.readUInt32BE(20) };
}
```

- [ ] **Step 5: 컬러 마스터 SVG를 그린다**

Create `src-tauri/icons/source/app-icon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="128" height="128" viewBox="0 0 128 128">
  <defs>
    <linearGradient id="plate" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#2E2E33"/>
      <stop offset="1" stop-color="#141416"/>
    </linearGradient>
  </defs>
  <rect width="128" height="128" rx="28" fill="url(#plate)"/>
  <g transform="rotate(-90 64 64)" fill="none" stroke-linecap="round">
    <circle cx="64" cy="64" r="47" stroke="#3A3A3C" stroke-width="14"/>
    <circle cx="64" cy="64" r="47" stroke="#D97757" stroke-width="14" stroke-dasharray="201 296"/>
    <circle cx="64" cy="64" r="22" stroke="#3A3A3C" stroke-width="14"/>
    <circle cx="64" cy="64" r="22" stroke="#5162ED" stroke-width="14" stroke-dasharray="58 139"/>
  </g>
</svg>
```

- [ ] **Step 6: 트레이용 단색 마스터 SVG를 그린다**

Create `src-tauri/icons/source/tray-template.svg`:

컬러 마스터와 같은 링이지만 판이 없다. 판이 주던 여백이 사라지므로 그림을 0.85배로 줄여 메뉴바에서 숨 쉴 틈을 남긴다. macOS 템플릿 아이콘은 색을 버리고 알파만 쓰므로, 트랙과 진행분은 색이 아니라 `stroke-opacity`로 구분한다.

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="128" height="128" viewBox="0 0 128 128">
  <g transform="translate(64 64) scale(0.85) translate(-64 -64)">
    <g transform="rotate(-90 64 64)" fill="none" stroke="#000000" stroke-linecap="round">
      <circle cx="64" cy="64" r="47" stroke-opacity="0.3" stroke-width="14"/>
      <circle cx="64" cy="64" r="47" stroke-opacity="1" stroke-width="14" stroke-dasharray="201 296"/>
      <circle cx="64" cy="64" r="22" stroke-opacity="0.3" stroke-width="14"/>
      <circle cx="64" cy="64" r="22" stroke-opacity="1" stroke-width="14" stroke-dasharray="58 139"/>
    </g>
  </g>
</svg>
```

- [ ] **Step 7: 렌더 스크립트를 만든다**

Create `scripts/icons/render.ts`:

```ts
/**
 * 마스터 SVG에서 아이콘 산출물을 만든다: `npm run icons`
 *
 * 렌더러는 이미 devDependency인 Playwright의 Chromium이라 새 의존성이 없다.
 * 여기서는 PNG만 뽑고, 번들 아이콘 세트(.ico/.icns/각 크기 PNG)는 이어서 도는
 * `tauri icon`이 1024px 결과물에서 파생한다.
 */

import { copyFile, mkdir, readFile, rm } from "node:fs/promises";
import { dirname } from "node:path";
import { chromium, type Page } from "playwright";
import { pngSize } from "./png.ts";

const SOURCE_DIR = "src-tauri/icons/source";
const TMP_DIR = "scripts/icons/.tmp";

const COLOR_MASTER = `${SOURCE_DIR}/app-icon.svg`;
const TRAY_MASTER = `${SOURCE_DIR}/tray-template.svg`;

type Job = { svg: string; size: number; out: string; why: string };

const JOBS: Job[] = [
  { svg: COLOR_MASTER, size: 1024, out: `${TMP_DIR}/app-icon-1024.png`, why: "tauri icon 입력" },
  { svg: COLOR_MASTER, size: 128, out: "docs/images/app-icon.png", why: "README" },
  { svg: TRAY_MASTER, size: 44, out: "src-tauri/icons/tray/tray-template.png", why: "macOS 트레이" },
];

/**
 * 둥근 모서리가 투명해야 OS가 아이콘을 제대로 마스킹하므로 배경을 비워 촬영한다.
 */
async function render(page: Page, job: Job): Promise<void> {
  const svg = await readFile(job.svg, "utf8");
  await page.setViewportSize({ width: job.size, height: job.size });
  await page.setContent(
    `<style>html,body{margin:0;padding:0;background:transparent}` +
      `svg{display:block;width:${job.size}px;height:${job.size}px}</style>${svg}`
  );
  await mkdir(dirname(job.out), { recursive: true });
  await page.screenshot({ path: job.out, omitBackground: true });

  const { width, height } = await pngSize(job.out);
  if (width !== job.size || height !== job.size) {
    throw new Error(`${job.out}: ${job.size}×${job.size}를 기대했는데 ${width}×${height}입니다.`);
  }
  console.log(`  ✓ ${job.out.padEnd(40)} ${job.size}px (${job.why})`);
}

async function main(): Promise<void> {
  await rm(TMP_DIR, { recursive: true, force: true });
  await mkdir(TMP_DIR, { recursive: true });

  const browser = await chromium.launch();
  try {
    const page = await browser.newPage();
    for (const job of JOBS) await render(page, job);
  } finally {
    await browser.close();
  }

  // favicon은 래스터가 아니라 마스터를 그대로 쓴다. 사본을 두는 쪽이
  // 빌드 설정에서 src-tauri를 들여다보게 만드는 것보다 낫다.
  await copyFile(COLOR_MASTER, "public/app-icon.svg");
  console.log(`  ✓ ${"public/app-icon.svg".padEnd(40)} 마스터 사본 (favicon)`);
}

main().catch((e) => {
  console.error("\n" + (e instanceof Error ? e.message : String(e)));
  process.exit(1);
});
```

- [ ] **Step 8: npm 스크립트와 .gitignore를 고친다**

`package.json`의 `scripts`에서 `screenshots` 줄 바로 뒤에 추가한다. 이 단계에서는 렌더까지만 배선하고, `tauri icon` 연결은 Task 2에서 한다.

```json
    "screenshots": "node scripts/screenshots/capture.ts",
    "icons": "node scripts/icons/render.ts",
```

`.gitignore`의 `scripts/screenshots/.tmp` 줄 아래에 추가한다:

```
# 아이콘 렌더 중간 산출물 (tauri icon 입력)
scripts/icons/.tmp
```

- [ ] **Step 9: 렌더를 돌린다**

Run: `npm run icons`
Expected: 네 줄의 `✓`가 찍히고 종료 코드 0.

- [ ] **Step 10: 눈으로 확인한다**

`docs/images/app-icon.png`와 `src-tauri/icons/tray/tray-template.png`를 연다. 확인할 것:
- 바깥 주황 링이 12시에서 시계 방향으로 약 2/3 차 있고, 안쪽 파란 링이 약 40% 차 있다.
- 판의 네 모서리가 투명하다 (흰색이나 검은색으로 채워져 있으면 `omitBackground`가 안 먹은 것이다).
- 트레이 템플릿은 배경이 완전히 투명하고 링만 검은 농담으로 남아 있다.

- [ ] **Step 11: 테스트가 통과하는지 확인한다**

Run: `npx vitest run scripts/icons/icons.test.ts`
Expected: 2 tests PASS

- [ ] **Step 12: 커밋한다**

`scripts/icons/.tmp`는 무시 대상이라 커밋되지 않는다. 의도한 것이다 — `tauri icon`의 입력일 뿐이다.

```bash
git add src-tauri/icons/source scripts/icons src-tauri/icons/tray docs/images/app-icon.png public/app-icon.svg package.json .gitignore
git commit -m "$(cat <<'EOF'
feat(icons): add icon masters and the SVG-to-PNG renderer

이중 링 게이지 마스터 SVG 두 장(컬러, 트레이용 단색)과 이를 PNG로 뽑는
Playwright 렌더 스크립트를 넣는다. 번들 아이콘 세트 재생성은 다음 커밋에서
tauri icon으로 잇는다.
EOF
)"
```

---

### Task 2: 번들 아이콘 세트 재생성

**Files:**
- Modify: `package.json` (`icons` 스크립트에 `tauri icon` 연결)
- Modify: `scripts/icons/png.ts` (픽셀 판독기 추가)
- Modify: `scripts/icons/icons.test.ts`
- Modify: `src-tauri/icons/*.png`, `src-tauri/icons/icon.ico`, `src-tauri/icons/icon.icns` (재생성)

**Interfaces:**
- Consumes: Task 1의 `pngSize()`, `scripts/icons/.tmp/app-icon-1024.png`
- Produces:
  - `pngPixel(path: string, x: number, y: number): Promise<Rgba>` — `Rgba`는 `{ r, g, b, a }`, 모두 0–255
  - 새 아이콘으로 교체된 `src-tauri/icons/` 번들 세트 16개 파일

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`scripts/icons/icons.test.ts`의 import 줄을 아래로 바꾼다:

```ts
import { describe, expect, it } from "vitest";
import { readdir } from "node:fs/promises";
import { pngPixel, pngSize } from "./png.ts";
```

그리고 파일 끝에 describe 블록을 하나 더 붙인다:

```ts
/**
 * 크기가 하나라도 빠지면 특정 플랫폼 빌드만 조용히 깨진다. 목록을 못 박아 둔다.
 */
const BUNDLE_FILES = [
  "128x128.png",
  "128x128@2x.png",
  "32x32.png",
  "Square107x107Logo.png",
  "Square142x142Logo.png",
  "Square150x150Logo.png",
  "Square284x284Logo.png",
  "Square30x30Logo.png",
  "Square310x310Logo.png",
  "Square44x44Logo.png",
  "Square71x71Logo.png",
  "Square89x89Logo.png",
  "StoreLogo.png",
  "icon.icns",
  "icon.ico",
  "icon.png",
];

describe("번들 아이콘 세트", () => {
  it("필요한 파일이 빠짐없이 있다", async () => {
    const entries = await readdir("src-tauri/icons", { withFileTypes: true });
    const files = entries.filter((e) => e.isFile()).map((e) => e.name);
    expect(files.sort()).toEqual([...BUNDLE_FILES].sort());
  });

  it("이름에 크기가 박힌 PNG는 실제 크기가 이름과 같다", async () => {
    for (const name of BUNDLE_FILES) {
      const match = name.match(/(\d+)x\1/);
      if (!match) continue;
      const expected = Number(match[1]) * (name.includes("@2x") ? 2 : 1);
      const { width, height } = await pngSize(`src-tauri/icons/${name}`);
      expect({ name, width, height }).toEqual({ name, width: expected, height: expected });
    }
  });

  /**
   * 파일 목록과 크기만으로는 세트가 *교체됐는지* 알 수 없다. 링이 지나는 자리의
   * 픽셀 색을 직접 확인해야 옛 아이콘이 남아 있는 것을 잡아낸다. 128px 아이콘은
   * 마스터와 좌표계가 1:1이라 샘플 지점을 계산할 필요가 없다.
   */
  const RING_SAMPLES = [
    { x: 111, y: 64, hex: "#D97757", what: "바깥 링 (Claude)" },
    { x: 86, y: 64, hex: "#5162ED", what: "안쪽 링 (Codex)" },
  ];

  it("링 위의 픽셀이 브랜드 색이다", async () => {
    for (const sample of RING_SAMPLES) {
      const px = await pngPixel("src-tauri/icons/128x128.png", sample.x, sample.y);
      const want = {
        r: parseInt(sample.hex.slice(1, 3), 16),
        g: parseInt(sample.hex.slice(3, 5), 16),
        b: parseInt(sample.hex.slice(5, 7), 16),
      };
      // 안티에일리어싱을 감안해 채널당 24까지 벌어지는 것은 허용한다.
      const off = Math.max(
        Math.abs(px.r - want.r),
        Math.abs(px.g - want.g),
        Math.abs(px.b - want.b)
      );
      expect({ what: sample.what, near: off <= 24 }).toEqual({ what: sample.what, near: true });
    }
  });

  it("판의 둥근 모서리가 투명하다", async () => {
    // 모서리가 불투명하면 macOS 독에서 아이콘이 각진 사각형으로 보인다.
    expect((await pngPixel("src-tauri/icons/128x128.png", 1, 1)).a).toBe(0);
  });
});
```

- [ ] **Step 2: PNG 픽셀 판독기를 만든다**

`scripts/icons/png.ts`의 import 줄을 바꾼다:

```ts
import { readFile } from "node:fs/promises";
import { inflateSync } from "node:zlib";
```

그리고 파일 끝에 추가한다:

```ts
export type Rgba = { r: number; g: number; b: number; a: number };

/**
 * 지정한 좌표의 픽셀을 읽는다. 8비트 논인터레이스 RGB(A) PNG만 다룬다 — 우리가
 * 만들고 검사하는 아이콘이 전부 그 형식이고, 그 대가로 이미지 라이브러리를
 * 들이지 않는다. 형식이 다르면 조용히 틀린 값을 주는 대신 예외를 던진다.
 */
export async function pngPixel(path: string, x: number, y: number): Promise<Rgba> {
  const buf = await readFile(path);
  if (buf.length < 33 || buf.readUInt32BE(0) !== 0x89504e47) {
    throw new Error(`${path}: PNG 파일이 아닙니다.`);
  }

  const width = buf.readUInt32BE(16);
  const height = buf.readUInt32BE(20);
  const depth = buf.readUInt8(24);
  const colorType = buf.readUInt8(25);
  const interlace = buf.readUInt8(28);
  if (depth !== 8 || interlace !== 0 || (colorType !== 2 && colorType !== 6)) {
    throw new Error(
      `${path}: 8비트 논인터레이스 RGB(A) PNG만 읽습니다 ` +
        `(depth=${depth}, colorType=${colorType}, interlace=${interlace}).`
    );
  }
  if (x < 0 || y < 0 || x >= width || y >= height) {
    throw new Error(`${path}: (${x}, ${y})는 ${width}×${height} 밖입니다.`);
  }

  const parts: Buffer[] = [];
  for (let off = 8; off + 8 <= buf.length; ) {
    const length = buf.readUInt32BE(off);
    const type = buf.toString("ascii", off + 4, off + 8);
    if (type === "IDAT") parts.push(buf.subarray(off + 8, off + 8 + length));
    if (type === "IEND") break;
    off += 12 + length; // 길이(4) + 타입(4) + 데이터 + CRC(4)
  }

  const bpp = colorType === 6 ? 4 : 3;
  const stride = width * bpp;
  const raw = inflateSync(Buffer.concat(parts));
  const out = Buffer.alloc(stride * (y + 1));

  // 각 줄은 앞줄과 왼쪽 픽셀을 참조하는 필터로 인코딩돼 있어, 원하는 줄까지
  // 순서대로 풀어야 한다 (PNG 명세 9.2).
  for (let row = 0; row <= y; row++) {
    const start = row * (stride + 1);
    const filter = raw[start];
    const cur = out.subarray(row * stride, (row + 1) * stride);
    const prev = row > 0 ? out.subarray((row - 1) * stride, row * stride) : null;
    for (let i = 0; i < stride; i++) {
      const left = i >= bpp ? cur[i - bpp] : 0;
      const up = prev ? prev[i] : 0;
      const upLeft = prev && i >= bpp ? prev[i - bpp] : 0;
      let value = raw[start + 1 + i];
      switch (filter) {
        case 0:
          break;
        case 1:
          value += left;
          break;
        case 2:
          value += up;
          break;
        case 3:
          value += (left + up) >> 1;
          break;
        case 4: {
          const p = left + up - upLeft;
          const dl = Math.abs(p - left);
          const du = Math.abs(p - up);
          const dul = Math.abs(p - upLeft);
          value += dl <= du && dl <= dul ? left : du <= dul ? up : upLeft;
          break;
        }
        default:
          throw new Error(`${path}: 알 수 없는 필터 타입 ${filter} (${row}번째 줄).`);
      }
      cur[i] = value & 0xff;
    }
  }

  const at = y * stride + x * bpp;
  return { r: out[at], g: out[at + 1], b: out[at + 2], a: bpp === 4 ? out[at + 3] : 255 };
}
```

- [ ] **Step 3: 테스트가 실패하는지 확인한다**

Run: `npx vitest run scripts/icons/icons.test.ts`
Expected: "링 위의 픽셀이 브랜드 색이다" FAIL — `src-tauri/icons/128x128.png`가 아직 Tauri 기본 아이콘이라 해당 좌표가 브랜드 색이 아니다. 이것이 이 작업이 붙잡는 변화다.

- [ ] **Step 4: `tauri icon`을 스크립트에 잇는다**

`package.json`의 `icons` 스크립트를 바꾼다:

```json
    "icons": "node scripts/icons/render.ts && tauri icon scripts/icons/.tmp/app-icon-1024.png -o src-tauri/icons",
```

- [ ] **Step 5: 세트를 재생성한다**

Run: `npm run icons`
Expected: 렌더 로그 뒤에 `tauri icon`이 각 파일을 쓰는 로그가 이어지고 종료 코드 0.

`tauri icon`이 `src-tauri/icons/source`나 `src-tauri/icons/tray` 디렉터리를 지우지 않았는지 확인한다:

```bash
ls src-tauri/icons/source src-tauri/icons/tray
```

Expected: 두 디렉터리와 그 안의 파일이 그대로 있다.

- [ ] **Step 6: 눈으로 확인한다**

`src-tauri/icons/32x32.png`와 `src-tauri/icons/128x128.png`를 연다. 새 이중 링 아이콘이어야 한다. 특히 32px에서 바깥 고리와 안쪽 심이 붙어 보이지 않고 따로 읽혀야 한다.

- [ ] **Step 7: 테스트가 통과하는지 확인한다**

Run: `npx vitest run scripts/icons/icons.test.ts`
Expected: 6 tests PASS

크기 테스트가 실패하면 **생성기를 의심하지 말고 목록을 실제 값에 맞춘다.** `tauri icon`이 어떤 크기를 내는지가 사실이고, 이 테스트는 그 사실을 고정하는 장치다.

모서리 투명도 테스트가 실패하면 그렇게 하지 않는다. 그것은 `tauri icon`이 알파를 뭉개고 있다는 뜻이고, macOS 독에서 아이콘이 각진 사각형으로 보이는 실제 결함이다. **값을 맞추지 말고 멈춰서 보고한다.**

- [ ] **Step 8: 생성이 결정적인지 확인한다**

같은 마스터로 두 번 돌리면 결과가 바이트 단위로 같아야 한다. 그래야 재생성이 불필요한 diff를 만들지 않는다.

```bash
find src-tauri/icons -maxdepth 1 -type f | sort | xargs sha256sum > /tmp/icons-a.txt
npm run icons
find src-tauri/icons -maxdepth 1 -type f | sort | xargs sha256sum > /tmp/icons-b.txt
diff /tmp/icons-a.txt /tmp/icons-b.txt && echo "결정적 ✓"
```

Expected: `결정적 ✓`

- [ ] **Step 9: 커밋한다**

```bash
git add package.json scripts/icons/png.ts scripts/icons/icons.test.ts src-tauri/icons
git commit -m "$(cat <<'EOF'
feat(icons): regenerate the bundle icon set from the new master

tauri icon을 npm run icons에 이어 붙이고 세트를 새 아이콘으로 재생성한다.
크기가 하나라도 빠지면 특정 플랫폼 빌드만 조용히 깨지므로, 파일 목록과
각 PNG의 크기를 테스트로 못 박는다.
EOF
)"
```

---

### Task 3: macOS 트레이 템플릿 아이콘

**Files:**
- Modify: `src-tauri/Cargo.toml:22` (tauri features)
- Modify: `src-tauri/src/lib.rs:81-85`

**Interfaces:**
- Consumes: Task 1이 만든 `src-tauri/icons/tray/tray-template.png`
- Produces: 없음 (앱 동작 변경으로 끝)

- [ ] **Step 1: `image-png` 기능을 켠다**

`tauri::include_image!`는 PNG 디코더를 필요로 한다. `src-tauri/Cargo.toml`의 tauri 줄을 바꾼다:

```toml
tauri = { version = "2", features = ["tray-icon", "macos-private-api", "image-png"] }
```

- [ ] **Step 2: 트레이 아이콘을 플랫폼별로 나눈다**

`src-tauri/src/lib.rs`에서 아래 블록을 찾는다:

```rust
            // Only set the icon if one is bundled; a missing icon shouldn't
            // panic app startup.
            if let Some(icon) = app.default_window_icon() {
                tray = tray.icon(icon.clone());
            }
```

이렇게 바꾼다:

```rust
            // macOS 메뉴바는 단색 템플릿 아이콘이 관례라 컬러 아이콘은 주변과 어긋난다.
            // 이미지는 컴파일 시점에 실행 파일에 박아 넣는다 — 런타임에 경로로 읽으면
            // 번들 리소스로 따로 실어야 하고, 그 배선이 빠지면 배포본에서만 깨진다.
            #[cfg(target_os = "macos")]
            {
                tray = tray
                    .icon(tauri::include_image!("icons/tray/tray-template.png"))
                    .icon_as_template(true);
            }
            // 다른 플랫폼은 창 아이콘을 그대로 쓴다. 아이콘이 없을 때 시작이 죽지
            // 않도록 하는 방어는 유지한다.
            #[cfg(not(target_os = "macos"))]
            {
                if let Some(icon) = app.default_window_icon() {
                    tray = tray.icon(icon.clone());
                }
            }
```

- [ ] **Step 3: 컴파일을 확인한다**

```bash
cd src-tauri && cargo check
```

Expected: `Finished` — 경고 없이 끝난다. `mut tray` 미사용 경고가 뜨면 분기 밖에서 `tray`를 다시 대입하지 않은 것이니 위 코드를 그대로 옮겼는지 확인한다.

**여기서 검증되는 것은 macOS가 아닌 분기뿐이다.** 개발 환경이 WSL이라 `#[cfg(target_os = "macos")]` 블록은 컴파일조차 되지 않는다. 이 공백은 Task 5에서 PR 본문에 남긴다.

- [ ] **Step 4: 기존 테스트가 깨지지 않았는지 확인한다**

```bash
cd src-tauri && cargo test
```

Expected: 기존 테스트 전부 PASS

- [ ] **Step 5: 커밋한다**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(tray): use a monochrome template icon on macOS

macOS 메뉴바는 단색 템플릿 아이콘이 관례라 컬러 아이콘이 주변과 어긋난다.
include_image!로 컴파일 시점에 박아 넣어 번들 리소스 배선 없이 동작하게 한다.
다른 플랫폼은 종전대로 창 아이콘을 재사용한다.

macOS 분기는 WSL에서 컴파일되지 않으므로 실물 확인이 남아 있다.
EOF
)"
```

---

### Task 4: 스캐폴딩 잔재 정리

**Files:**
- Modify: `index.html:5-7`
- Delete: `public/vite.svg`, `public/tauri.svg`, `src/assets/react.svg`

**Interfaces:**
- Consumes: Task 1이 만든 `public/app-icon.svg`
- Produces: 없음

- [ ] **Step 1: 정말 참조가 없는지 다시 확인한다**

지우기 전에 확인한다. 계획이 틀렸을 수도 있다.

```bash
grep -rn "vite.svg\|tauri.svg\|react.svg" --include="*.ts" --include="*.tsx" --include="*.html" --include="*.css" --include="*.json" . | grep -v node_modules
```

Expected: `index.html:5`의 `vite.svg` 한 줄만 나온다. 다른 참조가 나오면 **지우지 말고 멈춘 뒤 보고한다.**

- [ ] **Step 2: index.html을 고친다**

`index.html`의 5번 줄과 7번 줄을 바꾼다. 바꾸기 전:

```html
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tauri + React + Typescript</title>
```

바꾼 뒤:

```html
    <link rel="icon" type="image/svg+xml" href="/app-icon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Token Usage</title>
```

- [ ] **Step 3: 잔재 파일을 지운다**

```bash
git rm public/vite.svg public/tauri.svg src/assets/react.svg
rmdir src/assets 2>/dev/null || true
```

Expected: 세 파일이 삭제된다. `src/assets`가 비었으면 함께 사라지고, 다른 파일이 남아 있으면 `rmdir`이 조용히 실패하며 디렉터리는 유지된다 — 둘 다 정상이다.

- [ ] **Step 4: 빌드와 테스트를 확인한다**

```bash
npm run build && npm test
```

Expected: 빌드 성공, 테스트 전부 PASS. 빌드가 없는 에셋을 참조한다며 실패하면 Step 1의 grep이 놓친 참조가 있는 것이니 되돌리고 다시 확인한다.

- [ ] **Step 5: favicon이 실제로 뜨는지 확인한다**

```bash
npm run dev
```

브라우저에서 `http://localhost:1420`을 열고 탭 아이콘이 새 이중 링인지, 탭 제목이 `Token Usage`인지 본다. 확인 후 서버를 끈다.

- [ ] **Step 6: 커밋한다**

```bash
git add index.html public src
git commit -m "$(cat <<'EOF'
chore: drop Tauri scaffolding leftovers

favicon이 아직 vite.svg를, 문서 제목이 "Tauri + React + Typescript"를
가리키고 있었다. 새 아이콘으로 바꾸고, 어디서도 참조하지 않는
tauri.svg·react.svg·vite.svg를 지운다.
EOF
)"
```

---

### Task 5: README와 마무리

**Files:**
- Modify: `README.md:1`, `README.md:151-171` 사이에 절 추가, `README.md:173-179`(문서 목록)

**Interfaces:**
- Consumes: Task 1이 만든 `docs/images/app-icon.png`
- Produces: 없음

- [ ] **Step 1: 제목 줄에 아이콘을 넣는다**

`README.md`의 첫 줄을 바꾼다. 바꾸기 전:

```markdown
# Token Usage App
```

바꾼 뒤:

```markdown
# <img src="docs/images/app-icon.png" width="28" align="top" alt=""> Token Usage App
```

`alt`를 비워 두는 것은 의도적이다. 바로 옆에 제목 글자가 같은 뜻으로 있으므로, 화면 낭독기가 같은 말을 두 번 읽지 않게 한다.

- [ ] **Step 2: 아이콘 재생성 안내를 넣는다**

`## 스크린샷 다시 만들기` 절이 끝나는 곳(`scripts/screenshots/tauri-stub.ts`에 응답을 추가하세요.) 바로 뒤, `## 문서` 앞에 새 절을 넣는다:

````markdown
## 아이콘 다시 만들기

아이콘의 원본은 `src-tauri/icons/source/`의 SVG 두 장입니다. 컬러 마스터
(`app-icon.svg`)에서 번들 아이콘 세트와 README 이미지, favicon이 나오고,
단색 마스터(`tray-template.svg`)는 macOS 메뉴바 전용입니다.

```bash
npm run icons
```

Playwright의 Chromium으로 SVG를 PNG로 렌더한 뒤 `tauri icon`이 각 플랫폼의
크기(`.ico`, `.icns`, Windows Store 타일)를 파생합니다. 생성물은 빌드에 필요하므로
저장소에 커밋합니다. 색이나 비율을 바꾸려면 SVG만 고치고 이 명령을 다시 돌리세요 —
`src-tauri/icons/`의 파일을 손으로 고치면 다음 재생성 때 덮어써집니다.
````

- [ ] **Step 3: 문서 목록에 설계 문서를 추가한다**

`## 문서` 절의 목록 첫 두 줄 아래에 추가한다:

```markdown
- 아이콘 설계: `docs/superpowers/specs/2026-07-24-app-icon-design.md`
```

- [ ] **Step 4: 전체 검증을 돌린다**

```bash
npm test && npm run build && (cd src-tauri && cargo check)
```

Expected: 셋 다 성공. 하나라도 실패하면 출력 그대로 보고하고 멈춘다.

- [ ] **Step 5: 커밋한다**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: show the app icon in the README and document regeneration

아이콘을 제목 줄에 노출하고, 원본이 SVG 두 장이며 npm run icons로
재생성한다는 사실을 남긴다.
EOF
)"
```

- [ ] **Step 6: PR을 연다**

macOS 검증 공백을 본문에 반드시 남긴다. 이 사실이 빠지면 리뷰어가 확인된 것으로 오해한다.

```bash
git push -u origin donghoon-bigvalue/issue-59
gh pr create --title "feat: replace the scaffolding icon with the app's own" --body "$(cat <<'EOF'
Closes #59

Tauri 기본 아이콘을 앱을 대표하는 이중 링 게이지 아이콘으로 교체했습니다.
바깥 링이 Claude, 안쪽 링이 Codex고, 각 링의 채워진 호가 사용량을 뜻합니다.

## 바뀐 것

- `src-tauri/icons/source/`의 SVG 두 장이 유일한 원본. `npm run icons`로
  번들 세트 전체와 README 이미지, favicon을 재생성합니다.
- macOS 트레이는 단색 템플릿 아이콘으로 분기했습니다. 다른 플랫폼은 종전대로
  창 아이콘을 재사용합니다.
- favicon이 `vite.svg`를, 문서 제목이 `Tauri + React + Typescript`를 가리키던
  스캐폴딩 잔재를 정리하고 미사용 SVG 세 장을 지웠습니다.

## 확인한 것

- `npm test`, `npm run build`, `cargo check` 통과
- `npm run icons`를 두 번 돌려 산출물이 바이트 단위로 동일함 (결정적 생성)
- 렌더된 16·32·128px PNG 육안 확인 — 작은 크기에서도 두 링이 분리되어 읽힘

## 확인하지 못한 것

**macOS 메뉴바의 트레이 아이콘 실물.** 개발 환경이 WSL이라
`#[cfg(target_os = "macos")]` 블록은 컴파일조차 되지 않았습니다. macOS 빌드가
도는 CI나 실기에서 한 번 확인이 필요합니다.

설계 문서: `docs/superpowers/specs/2026-07-24-app-icon-design.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-Review

**Spec coverage**

| 설계 문서 절 | 담당 |
| --- | --- |
| 디자인 (수치 표) | Global Constraints, Task 1 Step 5 |
| 생성 파이프라인 | Task 1 (렌더), Task 2 (`tauri icon` 연결) |
| 결정적 생성 | Task 2 Step 8 |
| 트레이 아이콘 | Task 1 Step 6 (단색 마스터), Task 3 (Rust 배선) |
| 검증 공백 명시 | Task 3 Step 3, Task 5 Step 6 |
| 스캐폴딩 잔재 정리 표 | Task 4 |
| README 노출 | Task 5 Step 1–2 |
| 검증 항목 | Task 1 Step 10–11, Task 2 Step 6–8, Task 3 Step 3–4, Task 4 Step 4–5, Task 5 Step 4 |

빠진 요구사항 없음. `widget.html`을 손대지 않는다는 결정은 계획에 작업이 없는 것으로 반영된다.

**Placeholder scan**

TBD·TODO 없음. 코드를 바꾸는 모든 단계가 실제 코드를 담고 있고, 명령마다 기대 출력이 붙어 있다.

**Type consistency**

`pngSize(path: string): Promise<{ width: number; height: number }>`는 Task 1 Step 4에서 정의하고 Task 1 Step 2, Task 2 Step 1에서 같은 시그니처로 쓴다. `pngPixel(path, x, y): Promise<Rgba>`는 Task 2 Step 2에서 정의하고 같은 작업의 Step 1 테스트가 쓴다. 파일 경로는 전 작업에서 동일하다 — `src-tauri/icons/tray/tray-template.png`, `docs/images/app-icon.png`, `scripts/icons/.tmp/app-icon-1024.png`, `public/app-icon.svg`.

**실행 전 검토에서 고친 두 가지**

- Task 1의 테스트가 `.gitignore` 대상인 `scripts/icons/.tmp/app-icon-1024.png`를 검사해, 저장소를 새로 받은 사람과 CI에서 실패했을 것이다. 해당 검사를 뺐다. 1024px 크기 검증은 `render.ts`가 뽑는 즉시 스스로 한다.
- Task 2의 "실패하는 테스트"가 처음부터 통과해 아무 변화도 붙잡지 못했다. 링이 지나는 자리의 픽셀 색을 확인하는 테스트로 바꿔, 옛 아이콘이 남아 있으면 실제로 실패하게 했다. 이를 위해 `pngPixel`을 추가한다.
